@model IEnumerable<MovieForPosterViewModel>

@{
    ViewData["Title"] = "Home Page";
}


<div class="row mt-2">
    @foreach (MovieForPosterViewModel movie in Model)
    {
        @await Html.PartialAsync("_MovieForPoster", movie)
    }
</div>